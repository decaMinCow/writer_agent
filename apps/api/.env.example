# FastAPI
APP_ENV=dev
CORS_ALLOW_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
# Allow any localhost dev server port (fixes 400 preflight when Vite auto-picks 5174+).
CORS_ALLOW_ORIGIN_REGEX=^https?://(localhost|127[.]0[.]0[.]1)(:[0-9]+)?$

# Database (SQLAlchemy async URL)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/writer_agent

# OpenAI-compatible provider (fallback env config)
# You can also configure provider settings via the web UI (stored in DB) and leave these empty.
OPENAI_BASE_URL=
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_EMBEDDINGS_MODEL=text-embedding-3-small
OPENAI_TIMEOUT_S=60
