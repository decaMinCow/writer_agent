作品约束（Brief Snapshot，可为空对象）：
{{BRIEF_JSON}}

产物元信息：
{{ARTIFACT_META_JSON}}

重写指令：
{{INSTRUCTION}}

选中的原文（需要被替换）：
{{SELECTED_TEXT}}

选区前的上下文（可能截断）：
{{CONTEXT_BEFORE}}

选区后的上下文（可能截断）：
{{CONTEXT_AFTER}}

请输出替换文本（仅替换选区，不要包含其他内容）。

